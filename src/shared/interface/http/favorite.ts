export interface FavoriteResponse {
    id: number,
    productId: number,
    product: string,
    createdAt: string
}

export interface handleFavoriteResponse {
    id: number,
    productId: number,
    userId: number,
    createdAt: string
}